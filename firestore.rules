rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;  // Permitir lectura pública
      allow write: if true; // Permitir escritura pública (solo para desarrollo)
    }
  }
}